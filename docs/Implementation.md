This file list messages from our human testers - it's features or fixes to implement.
You will have to create a git branch if the current branch is main for that work and switch on
it.

## [DONE] We want to change the app icon.

You can use technics like transform to adapt the different sizes and have an optimized version that
Google want us to implement.
The icon image is at path "/Users/hb/Documents/Sutoko/Logo/app_logo.png" Take care it's 2MO

> Done on branch `feature/new-app-icon`: regenerated `ic_launcher_background` (artwork at 78%,
> feather-blended into a blurred edge fill, inside the 66dp safe zone) for mdpi→xxxhdpi, legacy
> `ic_launcher` (full-bleed) and `ic_launcher_round` (circle crop) at all densities, removed unused
> `ic_launcher_foreground`. Verified under circle + squircle masks (no seam).
> Validated with `:app:assembleDebug --no-build-cache`.