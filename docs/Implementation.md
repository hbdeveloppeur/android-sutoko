This file list messages from our human testers - it's features or fixes to implement.
You will have to create a git branch if the current branch is main for that work and switch on
it.
Do not touch :games:*

# Fix 1 [DONE]: Now a Sound Node may have a float "volume" field (optional). So you will have to change the players in SmsGameActivity

# Fix 2 [DONE]: In click to progress mode when playing a story, some node must not need a click to progress (Scene node must auto appear and continue, sounds too)