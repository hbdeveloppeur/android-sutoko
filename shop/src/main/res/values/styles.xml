<resources>

    <!-- Base application theme. -->
    <style name="shop_AppTheme" parent="Theme.MaterialComponents.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/shop_colorPrimary</item>
        <item name="colorPrimaryDark">@color/shop_colorPrimaryDark</item>
        <item name="colorAccent">@color/shop_colorAccent</item>
    </style>

    <!-- Main activity theme -->
    <style name="shop_SmsGameActivity2" parent="Theme.MaterialComponents.NoActionBar">
        <item name="colorPrimaryDark">@color/shop_black</item>
        <item name="android:windowBackground">@color/shop_midnight_red</item>
        <item name="android:fitsSystemWindows">true</item>
    </style>
</resources>
